# Leave blank to use the Vite dev proxy (recommended for local dev).
# Set only if you serve the frontend from a different origin than the API,
# e.g. VITE_API_BASE=http://localhost:8000
VITE_API_BASE=
